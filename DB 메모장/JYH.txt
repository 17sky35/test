1.
개념 - 전체적인 뷰
내부 - 데이터를 어디에 저장을 할것인가
외부 - 사용자 입장에서 어떻게 사용할 것인가
2.
CREATE TABLESPACE user_exam 
DATAFILE 'C:/Exam/user_exam.dbf' SIZE 10M;

3.
CREATE USER exam IDENTIFIED BY 1111;

4. 4번

5. 2번

6. 
ALTER TABLE ex02_10 ADD CONSTRAINT pk_ex02_10 PRIMARY KEY(col1);

7.
CREATE TABLE PRODUCT(
"NO" NUMBER PRIMARY KEY,
NAME VARCHAR2(100) NOT NULL,
PIRCE NUMBER,
P_DATE DATE
);

8. 
CREATE SEQUENCE SEQ_USER;
INSERT INTO PRODUCT VALUES(SEQ_USER.NEXTVAL, '컴퓨터',100,SYSDATE)
INSERT INTO PRODUCT VALUES(SEQ_USER.NEXTVAL, '세탁기',60,'21/04/15')
INSERT INTO PRODUCT VALUES(SEQ_USER.NEXTVAL, '냉장고',200,SYSDATE)
INSERT INTO PRODUCT VALUES(SEQ_USER.NEXTVAL, '에어컨',300,SYSDATE)
INSERT INTO PRODUCT VALUES(SEQ_USER.NEXTVAL, '오디오',20,'20/12/15')


9. 
UPDATE PRODUCT SET 
PRICE = PRICE + 20
WHERE "NO" = 1000;

10. 
DROP FROM PRODUCT
WHERE NAME = '세탁기';

11.
SELECT NAME, PRICE
FROM PRODUCT;

12.
SELECT * FROM PRODUCT
ORDER BY PRICE DESC;

13. 2번

14. 2번

15. 1번

16. 
삽입이상	: 새 데이터를 삽입하기위해 불필요한 데이터도 삽행해야되는 문제
갱신이상	: 중복 행 중 일부만 변경하여 데이터가 불일치하게되는 현상
삭제이상 : 행을 삭제하면 꼭 필요한 데이터까지 함께 삭제되는 문제

17.
SELECT REPLACE(FIRST_NAME,'el','**')
FROM EMPLOTEES
WHERE DEPARTMENT_ID = 50;

18.
SELECT EMPLOYEE_ID, FIRST_NAME, SALARY
FROM EMPLOTEES
WHERE LENGTH(FIRST_NAME)>=6;

19.
SELECT EMPLOYEE_ID,MOD(EMPLOYEE_ID,2) 
FROM EMPLOYEES;

20.
SELECT EMPLOYEE_ID, FIRST_NAME
FROM EMPLOYEES
WHERE MOD(EMPLOYEE_ID,2) = 0;